import { ProfileStatus } from "./ProfileStatus";
import { create } from "react-test-renderer";

describe("ProfileStatus component", () => {
  //   test("status from props should be in state", () => {
  //     const component = create(<ProfileStatus status="yo" updateUserStatus={() => {}} />);
  //     const instance = component.getInstance();
  //     expect(instance.state.status).toBe("yo");
  //   });
  //   test("status from props should be in the state", () => {
  //     const props = {
  //       status: "Test status",
  //       updateUserStatus: (status: string) => {},
  //     };
  //     // Используем ProfileStatus как JSX элемент для создания React элемента
  //     const component = create(<ProfileStatus status="yo" updateUserStatus={() => {}} />);
  //     const instance = component.root;
  //     expect(instance.props.status).toBe("Test status");
  //   });
});
